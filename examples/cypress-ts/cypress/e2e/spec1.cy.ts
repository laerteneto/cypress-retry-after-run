describe('Spec 1', () => {
  it('Test 1 - Pass', () => {
    expect(true).to.be.true;
  });

  it('Test 2 - Fail', () => {
    expect(false).to.be.true;
  });

  it('Test 3 - Pass', () => {
    expect(true).to.be.true;
  });
});
