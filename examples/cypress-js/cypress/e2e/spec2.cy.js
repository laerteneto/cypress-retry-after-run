describe('Spec 2', () => {
    it('passes 1', () => {
        expect(true).to.be.true
    })

    it('fails 2', () => {
        // Intentionally failing test
        expect(false).to.be.true
    })

    it('passes 3', () => {
        expect(true).to.be.true
    })
})
