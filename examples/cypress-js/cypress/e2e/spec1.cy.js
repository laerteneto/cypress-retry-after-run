describe('Spec 1', () => {
    it('passes 1', () => {
        expect(true).to.be.true
    })

    it('passes 2', () => {
        expect(true).to.be.true
    })

    it('fails 1', () => {
        // Intentionally failing test
        expect(true).to.be.false
    })
})
